// 함수 선언식과 표현식

console.log(sum(1,2));
console.log(mul2(3,4));
//console.log(mul(3,4));

function sum (a, b) {
  return a+b;
}

var mul = function mul2 (a, b) {
  return a*b;
}

// 익명 함수 표현식이나 기명 함수 표현식이나 선언은 나중에 함.
